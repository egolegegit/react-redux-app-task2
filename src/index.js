import React from 'react'
import ReactDOM from 'react-dom'

const App = (params) => {
 

  return <h1>Apps</h1>
}

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
)
